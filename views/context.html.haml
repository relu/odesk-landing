%section.container
  .row.page-headline
    .col-lg-12.text-center
      %h1 Trusted #{@ct}. Rated, Reviewed, Tested.

      %h3 Find the right #{@ct_singular} in <strong>3 easy steps</strong>
  .row
    .steps.col-lg-12.text-center
      .step-block.block-1
        .flipper
          .front
            %i.icon
            %span
              %strong.num 1.
              Post a Job
          .back
            %p
              Our time-tracking tools provide full visibility into your
              project's progress, so you'll know you're only paying for
              being done.
      .step-block.block-2
        .flipper
          .front
            %i.icon
            %span
              %strong.num 2.
              Review Applicants
          .back
            %p
              Peruse applicant portfolios and pick the most qualified
              candidates for your project.
      .step-block.block-3
        .flipper
          .front
            %i.icon
            %span
              %strong.num 3.
              Hire Right Away
          .back
            %p
              Hire your favorite candidate with a few quick clicks!

%section.form-section
  .container
    .form-headline
      %h1 Get #{@ct_singular} Applications Fast
    .row
      .col-lg-6.col-lg-offset-3
        %form.form{action: '/send', method: 'post'}
          .form-group
            %label{for: 'title'} Give your project a title
            %input#title.form-control{name: 'title', type: 'text', placeholder: 'Ex. Build a website'}
          .form-group
            %label{for: 'desc'} Describe what you want done
            %textarea#desc.form-control{name: 'desc', rows: 7, placeholder: "Enter details about your project and the skills you are looking for. (must be at least 50 characters)"}
          .form-group
            %label{for: 'email'} Enter your email
            %input#email.form-control{name: 'email', type: 'email'}
          .form-group.text-center
            = csrf_tag
            %input{type: 'hidden', name: 'scroll_count', value: 0}
            %input.btn.btn-lg.btn-submit{type: 'submit', value: 'Get Applicants Fast'}
            %p
              %em.terms
                By pressing 'Get Applicants Fast' you confirm that you accept the
                %br/
                %a{href: '#'} Terms and Conditions
                and
                %a{href: '#'} Privacy Policy

%section.contractors
  .wrapper
    .nav
      %a.left-arrow.disabled{href: '#'}
      %a.right-arrow.disabled{href: '#'}
    %ul.tiles
      - @profiles.each do |profile|
        %li.tile
          %article
            %header
              %h3= highlight(profile[:title], @keyword)
            .profile
              .desc
                %p= highlight(profile[:desc], @keyword)
              .skills
                - profile[:skills].each do |skill|
                  %span= highlight(skill, @keyword)
            %footer
              %img.image{src: profile[:portrait_50], alt: profile[:name]}
              .info
                .name= profile[:name]
                .country= profile[:country]
              .rate
                .dollars $#{'%.2f' % profile[:rate].to_f}
                .label hourly rate

      %li.tile.cta
        .content
          %strong
            %a{href: '/signup/create-account/'} Sign Up
          For FREE to see more great Developers

  .stats
    .container
      .row
        .col-lg-4.text-center.dev-count
          %strong 2,176
          = @ct
        .col-lg-4.text-center.complete-count
          %strong $ 69,735,263
          Projects completed
        .col-lg-4.text-center.project-count
          %strong 1,712,399
          Projects
